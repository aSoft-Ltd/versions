[versions]
atomicfu = "0.20.2" # https://maven.pkg.jetbrains.space/kotlin/p/wasm/experimental/org/jetbrains/kotlinx/atomicfu/
coroutines = "1.7.1"
serialization = "1.5.1"
datetime = "0.4.0"
# ktoml = "0.4.0" # consider removing this in favor of toml
toml = "0.3.6"
yaml = "0.12.0"
cli = "0.3.5"
bson = "0.5.0"

[libraries]
cli = { module="org.jetbrains.kotlinx:kotlinx-cli", version.ref = "cli" }

atomicfu = { module = "org.jetbrains.kotlinx:atomicfu", version.ref = "atomicfu" }

coroutines-core = { module = "org.jetbrains.kotlinx:kotlinx-coroutines-core", version.ref = "coroutines" }
coroutines-test = { module = "org.jetbrains.kotlinx:kotlinx-coroutines-test", version.ref = "coroutines" }
coroutines-android = { module = "org.jetbrains.kotlinx:kotlinx-coroutines-android", version.ref = "coroutines" }

serialization-core = { module = "org.jetbrains.kotlinx:kotlinx-serialization-core", version.ref = "serialization" }
serialization-json = { module = "org.jetbrains.kotlinx:kotlinx-serialization-json", version.ref = "serialization" }
serialization-bson = { module = "com.github.jershell:kbson", version.ref = "bson" }
serialization-yaml = { module = "net.mamoe.yamlkt:yamlkt", version.ref = "yaml" }
serialization-toml = { module = "net.peanuuutz.tomlkt:tomlkt", version.ref = "toml" }

# consider removing this in favor of serialization-toml
# serialization-toml-core = { module = "com.akuleshov7:ktoml-core", version.ref = "ktoml" }

datetime = { module = "org.jetbrains.kotlinx:kotlinx-datetime", version.ref = "datetime" }
